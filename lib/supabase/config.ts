/** Supabase Storage bucket name for PDF files */
export const DOCUMENTS_BUCKET = "documents";

/** PostgreSQL table name for document metadata */
export const DOCUMENTS_TABLE = "documents";

/** PostgreSQL table for text chunks (ready for embeddings / RAG) */
export const DOCUMENT_CHUNKS_TABLE = "document_chunks";
