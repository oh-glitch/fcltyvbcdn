/** Target size for each text chunk (characters). */
export const CHUNK_SIZE_CHARS = 1000;

/** Overlap between chunks so context is not lost at boundaries. */
export const CHUNK_OVERLAP_CHARS = 150;

/** Minimum chunk length worth saving. */
export const MIN_CHUNK_CHARS = 50;
